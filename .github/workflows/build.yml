name: Build unsigned IPA

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4 
      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.2.app
      - name: Show Xcode version
        run: xcodebuild -version
      - name: Build archive (unsigned)
        run: |
          set -o pipefail
          xcodebuild \
            -project Anonycord.xcodeproj \
            -scheme Anonycord \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -archivePath build/Anonycord.xcarchive \
            clean archive \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            DEVELOPMENT_TEAM=""

      - name: Package IPA
        run: |
          APP_PATH="build/Anonycord.xcarchive/Products/Applications/Anonycord.app"
          if [ ! -d "$APP_PATH" ]; then
            echo "::error::App not found at $APP_PATH - archive step failed"
            exit 1
          fi
          mkdir -p Payload
          cp -r "$APP_PATH" Payload/
          zip -qr Anonycord.ipa Payload
          echo "Built $(du -h Anonycord.ipa | cut -f1) IPA"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Anonycord-unsigned-ipa
          path: Anonycord.ipa

      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: Anonycord.ipa
